# Android release signing — copy this to android/key.properties and fill in your values
# NEVER commit key.properties to git

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=bushtrack
storeFile=../bushtrack-release.jks
