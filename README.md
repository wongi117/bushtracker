# bush_track

## Web Build

Set these environment variables for map rendering before building or deploying web:

- `MAPTILER_KEY`
- `MAPBOX_TOKEN`

Netlify uses them via `netlify.toml` during `flutter build web --release`.
